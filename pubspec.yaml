name: water_level_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.4.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  camera: ^0.10.5+9
  geolocator: ^11.0.0
  sqflite: ^2.3.0
  firebase_storage: ^12.3.5
  path: ^1.9.0
  mobile_scanner: ^5.0.0
  workmanager: ^0.9.0+3
  crypto: ^3.0.3
  http: ^1.2.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true